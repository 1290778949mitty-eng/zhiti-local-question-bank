# 项目工作规则

## 修改后的验证收尾

- 每次修改代码、排版或导出逻辑后，必须完成与修改内容直接对应的实际验证，再结束任务；仅通过编译或静态检查不算完成。
- 涉及 Word 导出时，必须从本地网页实际执行一次导出，检查生成 DOCX 的字体、公式、表格和图片结构，并将 DOCX 渲染成逐页图片进行视觉检查。
- 涉及网页显示或交互时，必须在本地网页中验证受影响的真实操作流程和结果。
- 如果验证发现问题，必须继续修改并重新执行完整验证；如果客观条件导致无法验证，必须明确说明，不能宣称修改已经完成。
