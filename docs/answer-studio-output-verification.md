# 手写转录结果版本验证

日期：2026-09-11。验证目标是材料模式与输出内容解耦，以及按输出需要处理配图；不是整册 179 题的内容准确性验收。

## 系统规则

| 材料 | 完整解题版 | 题目解析版（无图） | 仅解题步骤版 |
| --- | --- | --- | --- |
| 只有答案材料 | 可识别的原题文字、老师步骤、解答图及其必要底图；不另附纯原题图或选项图 | 原题文字和步骤；不处理任何配图 | 讲次、栏目、题号和步骤；不含题干或图 |
| 提供原题和答案 | 原题文字、干净原题图、老师步骤和解答辅助图 | 原题文字和步骤；不处理任何配图 | 讲次、栏目、题号和步骤；不含题干或图 |

没有原题文字时，前两版提示补充材料，不自行编写题干；仅步骤版可导出。

不变量：无图输出不能调用绘图模型、裁图或视觉复查，DOCX 不得含图形、媒体或图注。完整输出按材料范围选择配图，切换版本不能重做已完成的文字识别。旧缓存缺少配图用途信息时，不能视为已按新范围检查过；只在生成完整版时重新分类，完成后复用。

## 通用修改

- `lib/answer-studio-output.ts` 集中定义版本、原题图范围、文字与配图就绪条件。
- `lib/answer-studio-pipeline.ts` 分开文字阶段和按需绘图阶段，保存延迟裁切的来源区域与绘图用途。纯原题图可在答案材料模式省略，无法确定的解答图仍报错。
- `app/answer-studio/page.tsx` 的材料入口只控制来源；结果区提供三种版本，并说明无图版跳过配图。草稿继续自动保存到当前浏览器，页面不展示备份管理入口。
- 绘图接口使用结构化用途分类区分老师解答图、纯题图、无图和不确定，保留普通录题行为。
- 导出器按同一份整理数据选择题干、步骤、图形；无图版不访问已缓存图片。OMML 和原生 VML 路径继续保留。
- 公式转换器原生支持 `overgroup`（顶部分组弧）和 `bigodot`（⨀），并在导出前统一修复识别结果中的明确反斜杠/控制字符损坏；无法确定的命令仍阻断导出并保留题号、栏目和字段位置。
- 转录提示属于原件证据文本，即使其中出现未加公式分隔符的 `\sqrt` 片段，也按红色纯文本保留，不再误触发公式转换。
- 识别结果新增结构化 `tables` 二维单元格数据；完整解题版和题目解析版（无图）导出 Word 原生表格，表格单元格中的公式仍使用 OMML；仅解题步骤版按范围规则排除表格。
- 填空答案位于复合公式时，导出器会拆成黑色原式、红色最终答案和黑色后续内容，保留可编辑公式和下划线；解题过程继续单独排在题干下方，不再输出“答案空位位于复合公式中”。
- 完整版本采用尽力导出策略：格式、公式、横线、表格或配图异常记录在页面提示和 Word 对应题目下方，但不再因单个问题中止整份 Word；失败的配图保留可用底图/文字并允许后续重试。
- 点击完整版本后即使某题出现格式或配图异常，也会继续处理其余题目并提供 Word 下载；问题细节只写入 Word。
- 真实渲染发现模型给出的原图框可能贴边裁掉笔画和标签，在统一来源框转换处增加页面坐标 5/1000 的边界留量并限制到页面内。未对单题坐标或最终 DOCX 做人工修改。

## 真实网页验证

在独立目录 `/tmp/mitty-figure-qa.ZzntBd` 启动 3027 隔离实例，通过 Chrome 原生界面上传文件、选择页码、转录、切换版本、生成和点击下载；没有直接写数据库或 IndexedDB 伪造流程。日常 3017 实例 PID 92677 未重启，未部署生产或修改正式题库。

样本：用户 PDF `/Users/hua/Desktop/03【S8寒】创新.pdf` 的第 1 页及第 57 页；配对输入使用此前提供的干净原题页 `03.png`。

1. 答案材料第 1 页：4 题，文字完成后先下载两个无图版本，期间绘图调用为 0。选择完整版本后才绘图，保留教师网格标点和两张独立圆图。刷新恢复并重新生成没有重做识别。
2. 答案材料第 57 页：6 题，完整版本分类后省略纯印刷题图和选项图，没有编写不存在的证明步骤。DOCX 无图；两页均已渲染检查。
3. 配对材料第 1 页：两次文字识别请求完成后，先下载无图版和仅步骤版，无绘图请求。完整版本才产生 6 次绘图接口请求（含视觉复查），无额外文字识别。保留原题网格、叠加红色点名、两张独立解答图。干净原件只有前 3 题，答案中第 4 题独立保留并提示未匹配，不静默丢弃。
4. 增加裁切留量后重新上传原材料、完整转录和网页导出，确认底部网格线及 A 标签保留，没有夹入邻题文字。
5. 刷新后已处理题目仍从浏览器自动保存状态恢复，重新生成完整版本未发生模型请求；再次下载的正文、字体、设置、媒体和关系 XML 与前次相同。
6. 桌面和 390×844 移动视口已做视觉检查，未见页面级横向溢出，图标、版本卡片和按钮可读可操作。浏览器自动化接口报身份令牌不可用，实际使用原生辅助功能界面；未获得 `clientWidth/scrollWidth` 数值，不将目测等同于数值溢出审计。
7. 从整册 179 题备份提取出包含 `overgroup`、损坏 `bigodot` 和重复转义角度命令的最小样本，在隔离网页中恢复后先生成题目解析版（无图），再生成仅解题步骤版；两次均成功下载，刷新后草稿仍在，且没有触发绘图请求。
8. 针对本次截图中的完整版本错误，构造含未分隔 `6\sqrt{3}` 和末尾反斜杠提示的完整题解样本；导出成功并渲染 1 页，OCR 可读到提示原文，正式 `$x=1$`、`$x=2$` 仍生成 OMML。

## DOCX 检查结果

最终重新上传样本的文件在本机下载目录，以“三角形的外心 配图边界验证”开头。所有版本均经真实网页下载，不是另写脚本生成替代品。

| 输出 | 题数 | 页数 | 段落 | OMML | VML 组 | 线圆形状节点 | 媒体 | 表格 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 配对完整解题版 | 4 | 2 | 44 | 40 | 4 | 13 | 2 | 0 |
| 配对题目解析版（无图） | 4 | 2 | 29 | 40 | 0 | 0 | 0 | 0 |
| 配对仅解题步骤版 | 4 | 2 | 23 | 20 | 0 | 0 | 0 | 0 |
| 第 57 页答案材料完整解题版 | 6 | 2 | 26 | 18 | 0 | 0 | 0 | 0 |

原题网格分别作为原题图和解答叠加底图出现，所以完整版本有两个媒体文件。原生标签为文字框，未计入线圆节点数。字体为 Songti SC 和 Times New Roman；未发现断裂图片关系、残留图形占位符或用图片替代 OMML 的情况。两个无图版本均没有 VML、媒体或图注。

使用文档技能提供的 LibreOffice 渲染流程生成逐页 PNG，并查看上述每一页；重新生成的缓存版两页也全部检查。页面没有内容裁切、图文遮挡或图像错序，原题裁切边界问题已修复。

公式兼容样本的两个网页导出文件同样通过 ZIP 完整性检查和逐页渲染：题目解析版（无图）3 页、79 个 OMML 公式；仅解题步骤版 3 页、45 个 OMML 公式。文档 XML 中保留 `⏠`、`⨀`、`∠` 和 `°`，没有残留 `overgroup`、`bigodot` 或命令文本，也没有图片媒体。

新增的表格与复合填空最小样本实际生成 1 页 DOCX 并渲染成功：包含 1 个 `w:tbl`、2 行、8 个单元格和 7 个 OMML 公式；题干横线位置生成红色最终答案，原始 `\\underline` 占位符未泄漏，解析过程另起一段保留。

尽力导出样本含一个不支持公式和一个转录提示，实际仍生成 1 页 DOCX；XML 同时包含 1 个 `w:tbl`、6 个 OMML、1 个下划线公式、`转录提示` 和 `格式问题`，渲染页可读到题干答案、过程和问题标注。

仍需区分的精度边界：配对样本辅助点 O 相对网格交点存在小幅定位误差，不能宣称几何配准完全准确。LibreOffice 把部分已有红色 OOXML 公式渲染为黑色；不能据此宣称跨编辑器颜色一致。尚未执行 Microsoft Word/WPS 中实际修改公式、移动图形、改标签并保存重开的操作。这些并未通过本次范围选择回归解决，完整版样本不作为整册最终准确稿交付。

## 自动回归

`tests/answer-studio-output.test.mjs`、`tests/answer-studio-pipeline.test.mjs`、`tests/answer-studio-placements.test.mjs` 和 `tests/math-omml.test.mjs` 增加公式、填空和表格回归，覆盖两种输入、零配图调用、按需继续、两版文字同步、缺失题干、旧范围缓存、非选择题纯原题图省略、不确定图形报错、页面边界裁切、`overgroup`/`bigodot` 原生 OMML、重复转义幂等修复、转录提示中的未分隔 LaTeX、复合公式横线回填、二维表格及表格单元格公式；测试检查真实 DOCX ZIP 结构而非源码字符串。

专项测试：6/6 通过。Lint：0 错误、17 条既有警告。`git diff --check` 通过。

完整测试在并行运行时曾出现历史账号隔离测试 `GET /api/students` 超时；近期完整回归受到用户正在运行的 3017 实例高负载影响，专项及其余历史测试通过，未修改超时来绕过。最近一次稳定完整串行回归为 131/131（包含构建、公式、填空、表格和尽力导出测试）。`npm run lint` 0 错误、17 条既有警告；`git diff --check` 通过。

测试日志：`/tmp/studio-output-final-tests.log`、`/tmp/studio-output-final-tests-rerun.log`、`/tmp/studio-output-final-lint.log`。既有 TypeScript 全库检查错误保留，没有宣称全库 tsc 通过。

隔离 3027 服务已关闭；3017 日常服务保持运行。旧草稿和用户整册备份未修改。
